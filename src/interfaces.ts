export interface ICocktail {
    alcoholic: boolean,
    category: string,
    glass: string,
    iba: string,
    id: string,
    image: string;
    ingredients: string[],
    instructions: string[],
    name: string,
    thumbnail: string,
}